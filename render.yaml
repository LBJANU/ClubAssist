services:
  - type: web
    name: clubassist
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn clubassist.wsgi"
    nginx:
      clientMaxBodySize: 50m
